---
includes:
  - local.yaml

extra_dist:
  - local.yaml

project:
  name: yambs2

dependencies:
  - github: {repo: yambs-sample2, owner: libre-embedded}
  - github: {repo: yambs-sample, owner: libre-embedded}
